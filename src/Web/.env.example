# Dev-only: Vite proxies /api to this backend URL
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:5080

# Production build: absolute API base (leave empty in dev to use the proxy)
# VITE_API_BASE_URL=https://api.example.com
